---
paths:
  - "backend/src/main/java/**/appeal/**/*.java"
---

# Appeal workflow

`min-days-auto` is intentionally shorter than `min-days-manual`: auto-bans exist to save
moderator time, so appealing one shouldn't cost more of it via a longer mandatory wait.
